# copy this file to .env and fill in your own values
# (dont commit the real .env file!)

DATABASE_URL=sqlite:///./expense_tracker.db
SECRET_KEY=change_this_to_something_random_later
ACCESS_TOKEN_EXPIRE_MINUTES=60
