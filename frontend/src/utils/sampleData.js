// this file used to hold fake expenses for building the UI before the
// backend was connected. not needed anymore now that everything reads
// from the api - just keeping the sample budget until phase 8 adds the
// real budget endpoint

export const sampleBudget = {
  monthly_limit: 8000,
};
